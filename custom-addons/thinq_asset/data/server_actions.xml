<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Server Action: Print Asset Labels -->
        <record id="action_print_asset_labels" model="ir.actions.server">
            <field name="name">Print Labels</field>
            <field name="model_id" ref="account_asset.model_account_asset"/>
            <field name="state">code</field>
            <field name="code">
# Open wizard untuk memilih layout dan quantity
view_id = env.ref('thinq_asset.view_asset_label_layout_wizard')
action = {
    'name': 'Print Asset Labels',
    'type': 'ir.actions.act_window',
    'res_model': 'asset.label.layout',
    'view_mode': 'form',
    'view_id': view_id.id,
    'target': 'new',
    'context': {
        'default_asset_ids': [(6, 0, records.ids)],
        'active_ids': records.ids,
        'active_model': 'account.asset',
    }
}
            </field>
            <field name="binding_model_id" ref="account_asset.model_account_asset"/>
            <field name="binding_view_types">list</field>
        </record>
    </data>
</odoo>