<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Search Invoice Menu -->
        <record id="menu_search_invoices" model="ir.ui.menu">
            <field name="name">Search Invoice</field>
            <field name="parent_id" ref="account.menu_finance_receivables"/>
            <field name="action" ref="action_search_invoices"/>
            <field name="sequence">16</field>
            <field name="groups_id" eval="[(6, 0, [ref('account.group_account_invoice')])]"/>
        </record>

        <!-- Search Bills Menu -->
        <record id="menu_search_bills" model="ir.ui.menu">
            <field name="name">Search Bills</field>
            <field name="parent_id" ref="account.menu_finance_payables"/>
            <field name="action" ref="action_search_bills"/>
            <field name="sequence">16</field>
            <field name="groups_id" eval="[(6, 0, [ref('account.group_account_invoice')])]"/>
        </record>

        <!-- Search Journal Entry Menu -->
        <record id="menu_search_journal_entry" model="ir.ui.menu">
            <field name="name">Search Journal Entry</field>
            <field name="parent_id" ref="account.menu_finance_entries"/>
            <field name="action" ref="action_search_journal_entry"/>
            <field name="sequence">16</field>
            <field name="groups_id" eval="[(6, 0, [ref('account.group_account_user')])]"/>
        </record>

        <!-- Search Journal Items Menu -->
        <record id="menu_search_journal_items" model="ir.ui.menu">
            <field name="name">Search Journal Items</field>
            <field name="parent_id" ref="account.menu_finance_entries"/>
            <field name="action" ref="action_search_journal_items"/>
            <field name="sequence">17</field>
            <field name="groups_id" eval="[(6, 0, [ref('account.group_account_user')])]"/>
        </record>

        <!-- Search Sales Orders Menu -->
        <record id="menu_search_sales_orders" model="ir.ui.menu">
            <field name="name">Search Sales Orders</field>
            <field name="parent_id" ref="sale.sale_order_menu"/>
            <field name="action" ref="action_search_sales_orders"/>
            <field name="sequence">15</field>
            <field name="groups_id" eval="[(6, 0, [ref('sales_team.group_sale_salesman')])]"/>
        </record>

        <!-- Search Purchase Orders Menu -->
        <record id="menu_search_purchase_orders" model="ir.ui.menu">
            <field name="name">Search Purchase Orders</field>
            <field name="parent_id" ref="purchase.menu_purchase_root"/> <!-- menu_purchase_form_action -->
            <field name="action" ref="action_search_purchase_orders"/>
            <field name="sequence">15</field>
            <field name="groups_id" eval="[(6, 0, [ref('purchase.group_purchase_user')])]"/>
        </record>

        <!-- Search POS Orders Menu -->
        <record id="menu_search_pos_orders" model="ir.ui.menu">
            <field name="name">Search Point of Sale</field> 
            <field name="parent_id" ref="point_of_sale.menu_point_root"/>
            <field name="action" ref="action_search_pos_orders"/>
            <field name="sequence">15</field>
            <field name="groups_id" eval="[(6, 0, [ref('point_of_sale.group_pos_user')])]"/>
        </record>

        <!-- Search Purchase Requests Menu -->
        <record id="menu_search_purchase_requests" model="ir.ui.menu">
            <field name="name">Search Purchase Requests</field>
            <field name="parent_id" ref="purchase_request.parent_menu_purchase_request"/> <!-- menu_purchase_request -->
            <field name="action" ref="action_search_purchase_requests"/>
            <field name="sequence">15</field>
            <field name="groups_id" eval="[(6, 0, [ref('purchase_request.group_purchase_request_user')])]"/>
        </record>
    </data>
</odoo>